import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const ResetPassword = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Passwort zurücksetzen</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Neues Passwort setzen Formular kommt hier</p>
      </CardContent>
    </Card>
  );
};

export default ResetPassword;